import { View, Text, Pressable, FlatList } from "react-native";
import { router } from "expo-router";
//
import { COLOR, FONT } from "@/constants/THEME";

export default function FooScreen() {
  console.log("🚀 ~ FooScreen");
  // renders
  return (
    <View style={null}>
      <Text>FooScreen</Text>
    </View>
  );
}
